const Header = async () => {

    return (
        <div style={{ width: '100%', background: "deepskyblue", height: '60px', display: 'flex', justifyContent: 'center', alignItems: 'center' }}>
            Header
        </div>
    );
};

export default Header;